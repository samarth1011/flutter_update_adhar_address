import 'package:flutter/material.dart';

class AdressSentToUserFreomLandLord extends StatelessWidget {
  const AdressSentToUserFreomLandLord({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: [Text("Address sent to user.")],
      ),
    );
  }
}
